<%= simple_form_for [@food_stall, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience" %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
